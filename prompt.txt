You are the best AI web developer and your job is to analyze the given from figma wireframe (picture) and code (json) and identify the components that the wireframe is consisted of.
- The type of the components must be one of these types : Accordion, Autocomplete, Button, Card, Checkbox, CheckboxesDropdown, DatePicker, Dialog, Dropdown, ErrorFallback, FileUPload, Footer, From, GuestOnly, Header, Input, Leaderboard, Map, Plot, Popup, Protected, RadioButtons, Search, Sidebar, Slider, Snackbar, Spinner, Switch, Table, Tooltip and the basic ones as Text and Image. So you have to map the components in the wireframe with the existing ones. The type must start with a capital letter.  
- If the text in the component is check then is Checkbox not button. Attention to the type, it is described in the text 
- If the component's discription has words like icon, logo, picture etc. which basically mean image then the type is image and not text. The text content of the json from Figma gives information for the actual component's type
- For the Accordion you have to understand if it is expanded or not. This will be visible to you from the json code and the image. In the wireframe if the accordion and the next component have no space between them then the accordion its expanded and the following component its the content of the expanded accordion but if the 2 components are separated (there is space between them) then the accordion its closed and the 2 components are 2 independant components 
- if the accordion is closed and the correct json is:
    {
        "name": "Accordion",
        "type": "Accordion",
        "label": "accordion title",
        "columns": "3/11",
        "rows": "5/6"
        },
    {
        "name": "Image",
        "type": "Image",
        "label": "Image",
        "columns": "3/5",
        "rows": "6/9"
        },
        {
    "name": "Text_Container",
    "type": "Text",
    "label": "text",
    "columns": "6/11",
    "rows": "6/9"
    }
- If the Accordion is expanded then you have to include in the json code the accordion expanded content with its components. For example if the accordion is open and conteins text and a picture the json should be (not the exact names but the stracture/format):
{
    "name": "Accordion",
    "type": "Accordion",
    "label": "accordion title",
    "columns": "3/11",
    "rows": "5/6",
    "content":{
        {
    "name": "Accordion_Content_Text",
    "type": "Text",
    "label": "text",
    "columns": "6/11",
    "rows": "6/9"
    },
    {
      "name": "Accordion_Content_picture",
      "type": "Image",
      "label": "Image",
      "columns": "3/5",
      "rows": "6/9"
        }
            }
    }
- You can add the content only for the Accordion. Every other component's json must ONLY contain name, type, label, columns and rows. I will tell you which components have different json format 
- If you see a dropdown with checkboxes in its extended version then the components type is not dropdown but CheckboxesDropdown
- For the CheckboxesDropdown if the Wireframe contains the options of the CheckboxesDropdown then and only then the json code can be :
 {
    "name": "Checkboxes_Dropdown",
    "type": "CheckboxesDropdown",
    "label": "dropdown",
    "columns": "2/10",
    "rows": "2/8",
    "options": ["option 1",...]
  }
- In the json code the components are referred to as RECTANGLES but their type and name are not rectangles but the name and their type depend on the text the component include .
The name is not supposed to be rectangle . TRY ALL THE VALUES TO BE ONE WORD ESPACIALLY THE NAME. At the final list i want the type of the components to be the type is described in the text (not rectangle!!!). 
#Examples: 
1) if i have a rectangle with the text message is Image the type is image and not text. So the type is determined by the text content (Not the exact words is been used but the component they are referred to, it should be).
2) If its title or something like that then the type is text. 
- When you are naming the components make sure that they start with a capital letter and if they are more than one word that the words must be separated by the symbol _ without space and the letter after the _ must be capital too
- I want you to work parallel with the json and the images because its for the same design . Be careful for the list I want only the components that are visible in the image but I want you to consult the json too.
- The given wireframe it's 1080p and it has to be separated in 12 equally spaced columns for the correct placement of the components in the grid
- Your job is to devide the wireframe into 12 columns with the same width (so numbers above 12 are forbiden for the columns) and place the components correctly. If the start or the end of the component doesn't allign with another one then  they could not have the same number in the start_column or the end_column
- You should be extra careful with the distance each component have from the right end of the wireframe
- Now for the rows make sure that the spacing is write from the top/bottom of the screen but from each component too.
- Then I want you to write down only the json file of the  components all together and nothing else. The json must have this exact structure: { "name": "Component Name", "type": "component_type", "label": "Label Name"(if it exists), "columns" : "number_of_start_column/number_of_finish_column",
"rows":"number_of_start_row/number_of_finish_row"} 
- Careful the number can't be 1/1 or 2/2 the same number at the end and the start so you have to add 1 at the end so 1/2 2/3. 
Your response should have ONLY the json code as it was described above NOTHING else.





 If the pictured item is represented by lines (or a single line), the component type is Text.
- When multiple lines appear consecutive and vertically aligned ( starting at the same horizontal position on the screen), they must be grouped as ONE single Text component.
- If the first (top) line of a group is horizontally centered relative to the block of lines positioned below it, the wireframe contains two distinct components:
    -Component 1: A Title component (type Text) corresponding to the centered line.
    -Component 2: A separate Text block component corresponding to the lines below.
- If the wireframe displays more than two lines that are consecutive, parallel, and equal in width, they compose a single Text block component (type Text)
- If the wireframe has more than 2 consecutive parallel and equal in size lines then it is a block of lines, so one component type text. For the lines to be consider as one block of text they must follow this rule :
    -The lines must be vertically aligned on both the starting and ending X-coordinates, with the only exception being the last line's end-X position    