For the installation the requirements are in the requirements.txt
If you want the LLM to do the placement too, the LLM_chat_2.py is ready 
If you want the deterministic way of placement, the LLM_chat.py needs the input files (images and json in the begining) and you need to change the render_grid_placement
with the one with the comments and uncomment the functions map_x_to_collumn and map_y_to_row
